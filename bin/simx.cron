test -f "$HOME/.amazon_keys" && . "$HOME/.amazon_keys"

env \
  PATH=/usr/local/bin:$PATH \
  RUBYOPT=rubygems \
  JRUBY_OPTS=-rubygems \
  bin/onboot var/simx.started rake stop start 1>var/simx.onboot.out 2>var/simx.onboot.err
